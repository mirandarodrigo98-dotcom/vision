[1mdiff --git a/src/app/actions/integrations/eklesia.ts b/src/app/actions/integrations/eklesia.ts[m
[1mindex c539e66..c60541c 100644[m
[1m--- a/src/app/actions/integrations/eklesia.ts[m
[1m+++ b/src/app/actions/integrations/eklesia.ts[m
[36m@@ -1549,8 +1549,14 @@[m [mexport async function exportTransactionsCsv([m
         const header = ['Data', 'Categoria', 'Natureza', 'Histórico', 'Valor', 'Conta', 'Cód. Conta'];[m
         const rows = transactions.map(t => [[m
             t.date ? (() => {[m
[31m-              const d = new Date(t.date + 'T12:00:00');[m
[31m-              return !isNaN(d.getTime()) ? d.toLocaleDateString('pt-BR') : '';[m
[32m+[m[32m              try {[m
[32m+[m[32m                const parsed = t.date instanceof Date[m[41m [m
[32m+[m[32m                  ? t.date[m[41m [m
[32m+[m[32m                  : (typeof t.date === 'string' ? (t.date.includes('T') ? new Date(t.date) : new Date(t.date + 'T12:00:00')) : new Date(t.date));[m
[32m+[m[32m                return !isNaN(parsed.getTime()) ? parsed.toLocaleDateString('pt-BR') : '';[m
[32m+[m[32m              } catch (e) {[m
[32m+[m[32m                return '';[m
[32m+[m[32m              }[m
             })() : '',[m
             t.category_name || '',[m
             t.category_nature || '',[m
[1mdiff --git a/src/components/integrations/eklesia/csv-import-dialog.tsx b/src/components/integrations/eklesia/csv-import-dialog.tsx[m
[1mindex 8d42fdc..684b7e2 100644[m
[1m--- a/src/components/integrations/eklesia/csv-import-dialog.tsx[m
[1m+++ b/src/components/integrations/eklesia/csv-import-dialog.tsx[m
[36m@@ -253,7 +253,9 @@[m [mexport function CsvImportDialog({ companyId, onSuccess }: CsvImportDialogProps)[m
                                                   {item.date ? ([m
                                                     (() => {[m
                                                       try {[m
[31m-                                                        const parsed = typeof item.date === 'string' && item.date.includes('T') ? new Date(item.date) : new Date(item.date + 'T12:00:00');[m
[32m+[m[32m                                                        const parsed = item.date instanceof Date[m[41m [m
[32m+[m[32m                                                          ? item.date[m[41m [m
[32m+[m[32m                                                          : (typeof item.date === 'string' ? (item.date.includes('T') ? new Date(item.date) : new Date(item.date + 'T12:00:00')) : new Date(item.date));[m
                                                         return isValid(parsed) ? format(parsed, 'dd/MM/yyyy') : '-';[m
                                                       } catch (e) {[m
                                                         return '-';[m
[36m@@ -278,7 +280,9 @@[m [mexport function CsvImportDialog({ companyId, onSuccess }: CsvImportDialogProps)[m
                                               {item.date ? ([m
                                                 (() => {[m
                                                   try {[m
[31m-                                                    const parsed = typeof item.date === 'string' && item.date.includes('T') ? new Date(item.date) : new Date(item.date + 'T12:00:00');[m
[32m+[m[32m                                                    const parsed = item.date instanceof Date[m[41m [m
[32m+[m[32m                                                      ? item.date[m[41m [m
[32m+[m[32m                                                      : (typeof item.date === 'string' ? (item.date.includes('T') ? new Date(item.date) : new Date(item.date + 'T12:00:00')) : new Date(item.date));[m
                                                     return isValid(parsed) ? format(parsed, 'dd/MM/yyyy') : '-';[m
                                                   } catch (e) {[m
                                                     return '-';[m
[1mdiff --git a/src/components/integrations/eklesia/transactions-manager.tsx b/src/components/integrations/eklesia/transactions-manager.tsx[m
[1mindex 9d4a4b5..682031a 100644[m
[1m--- a/src/components/integrations/eklesia/transactions-manager.tsx[m
[1m+++ b/src/components/integrations/eklesia/transactions-manager.tsx[m
[36m@@ -255,22 +255,41 @@[m [mexport function TransactionsManager({ companyId }: TransactionsManagerProps) {[m
     }[m
   };[m
 [m
[32m+[m[32m  const safeFormatDate = (dateVal: any) => {[m
[32m+[m[32m    if (!dateVal) return '-';[m
[32m+[m[32m    try {[m
[32m+[m[32m      let parsed: Date;[m
[32m+[m[32m      if (dateVal instanceof Date) {[m
[32m+[m[32m        parsed = dateVal;[m
[32m+[m[32m      } else if (typeof dateVal === 'string') {[m
[32m+[m[32m        // Handle "YYYY-MM-DD" or "YYYY-MM-DD HH:mm:ss" or ISO strings[m
[32m+[m[32m        const cleanDateStr = dateVal.trim();[m
[32m+[m[32m        if (cleanDateStr.includes('T')) {[m
[32m+[m[32m          parsed = new Date(cleanDateStr);[m
[32m+[m[32m        } else if (cleanDateStr.length === 10) {[m
[32m+[m[32m          // YYYY-MM-DD[m
[32m+[m[32m          parsed = new Date(cleanDateStr + 'T12:00:00');[m
[32m+[m[32m        } else {[m
[32m+[m[32m          // Maybe YYYY-MM-DD HH:mm:ss[m
[32m+[m[32m          parsed = new Date(cleanDateStr.replace(' ', 'T'));[m
[32m+[m[32m        }[m
[32m+[m[32m      } else {[m
[32m+[m[32m        parsed = new Date(dateVal);[m
[32m+[m[32m      }[m
[32m+[m[32m      return isValid(parsed) ? format(parsed, 'dd/MM/yyyy') : '-';[m
[32m+[m[32m    } catch (e) {[m
[32m+[m[32m      return '-';[m
[32m+[m[32m    }[m
[32m+[m[32m  };[m
[32m+[m
   const getPeriodText = () => {[m
     const f = filters as any;[m
     [m
[31m-    const safeFormat = (dateStr: string) => {[m
[31m-      try {[m
[31m-        const parsed = typeof dateStr === 'string' && dateStr.includes('T') ? new Date(dateStr) : new Date(dateStr + 'T12:00:00');[m
[31m-        if (isValid(parsed)) return format(parsed, 'dd/MM/yyyy');[m
[31m-      } catch (e) {}[m
[31m-      return '-';[m
[31m-    };[m
[31m-[m
     if (f.startDate && f.endDate) {[m
[31m-        return `${safeFormat(f.startDate)} a ${safeFormat(f.endDate)}`;[m
[32m+[m[32m        return `${safeFormatDate(f.startDate)} a ${safeFormatDate(f.endDate)}`;[m
     }[m
     if (syncStats?.minDate && syncStats?.maxDate) {[m
[31m-        return `${safeFormat(syncStats.minDate)} a ${safeFormat(syncStats.maxDate)}`;[m
[32m+[m[32m        return `${safeFormatDate(syncStats.minDate)} a ${safeFormatDate(syncStats.maxDate)}`;[m
     }[m
     return 'Todos os lançamentos filtrados';[m
   };[m
[36m@@ -297,11 +316,11 @@[m [mexport function TransactionsManager({ companyId }: TransactionsManagerProps) {[m
                     Excluir Selecionados ({selectedIds.length})[m
                 </Button>[m
             )}[m
[31m-            <Button onClick={handleSyncClick} variant="outline" disabled={isLoading || isSyncing || isExporting}>[m
[32m+[m[32m            <Button onClick={handleSyncClick} variant="outline" disabled={isLoading || isSyncing || isExporting || transactions.length === 0}>[m
                 <RefreshCw className={`mr-2 h-4 w-4 ${isSyncing ? 'animate-spin' : ''}`} />[m
                 Sincronizar Questor[m
             </Button>[m
[31m-            <Button onClick={handleExport} variant="outline" disabled={isLoading || isExporting || isSyncing}>[m
[32m+[m[32m            <Button onClick={handleExport} variant="outline" disabled={isLoading || isExporting || isSyncing || transactions.length === 0}>[m
                 <Download className="mr-2 h-4 w-4" />[m
                 Exportar CSV[m
             </Button>[m
[36m@@ -513,16 +532,7 @@[m [mexport function TransactionsManager({ companyId }: TransactionsManagerProps) {[m
                     />[m
                   </TableCell>[m
                   <TableCell>[m
[31m-                    {t.date ? ([m
[31m-                      (() => {[m
[31m-                        try {[m
[31m-                          const parsed = typeof t.date === 'string' && t.date.includes('T') ? new Date(t.date) : new Date(t.date + 'T12:00:00');[m
[31m-                          return isValid(parsed) ? format(parsed, 'dd/MM/yyyy') : '-';[m
[31m-                        } catch (e) {[m
[31m-                          return '-';[m
[31m-                        }[m
[31m-                      })()[m
[31m-                    ) : '-'}[m
[32m+[m[32m                    {safeFormatDate(t.date)}[m
                   </TableCell>[m
                   <TableCell>[m
                     <div className="flex flex-col">[m
[1mdiff --git a/src/components/integrations/enuves/transactions-manager.tsx b/src/components/integrations/enuves/transactions-manager.tsx[m
[1mindex f3ed04b..0497774 100644[m
[1m--- a/src/components/integrations/enuves/transactions-manager.tsx[m
[1m+++ b/src/components/integrations/enuves/transactions-manager.tsx[m
[36m@@ -297,11 +297,11 @@[m [mexport function TransactionsManager({ companyId }: TransactionsManagerProps) {[m
                     Excluir Selecionados ({selectedIds.length})[m
                 </Button>[m
             )}[m
[31m-            <Button onClick={handleSyncClick} variant="outline" disabled={isLoading || isSyncing || isExporting}>[m
[32m+[m[32m            <Button onClick={handleSyncClick} variant="outline" disabled={isLoading || isSyncing || isExporting || transactions.length === 0}>[m
                 <RefreshCw className={`mr-2 h-4 w-4 ${isSyncing ? 'animate-spin' : ''}`} />[m
                 Sincronizar Questor[m
             </Button>[m
[31m-            <Button onClick={handleExport} variant="outline" disabled={isLoading || isExporting || isSyncing}>[m
[32m+[m[32m            <Button onClick={handleExport} variant="outline" disabled={isLoading || isExporting || isSyncing || transactions.length === 0}>[m
                 <Download className="mr-2 h-4 w-4" />[m
                 Exportar CSV[m
             </Button>[m
[1mdiff --git a/src/app/actions/integrations/questor.ts b/src/app/actions/integrations/questor.ts[m
[1mindex a6754ca..99b9bf3 100644[m
[1m--- a/src/app/actions/integrations/questor.ts[m
[1m+++ b/src/app/actions/integrations/questor.ts[m
[36m@@ -409,16 +409,16 @@[m [mexport async function checkEklesiaQuestorSyncStatus(companyId: string, filters:[m
     const result = await db.prepare(query).get(...params) as any;[m
     [m
     return {[m
[31m-      total: result.total || 0,[m
[31m-      synced: result.synced || 0,[m
[31m-      pending: result.pending || 0,[m
[31m-      minDate: result.min_date,[m
[31m-      maxDate: result.max_date,[m
[31m-      hasPriorSync: (result.synced || 0) > 0[m
[32m+[m[32m      total: Number(result?.total || 0),[m
[32m+[m[32m      synced: Number(result?.synced || 0),[m
[32m+[m[32m      pending: Number(result?.pending || 0),[m
[32m+[m[32m      minDate: result?.min_date,[m
[32m+[m[32m      maxDate: result?.max_date,[m
[32m+[m[32m      hasPriorSync: Number(result?.synced || 0) > 0[m
     };[m
[31m-  } catch (error) {[m
[31m-    console.error('Error checking sync status (Eklesia):', error);[m
[31m-    return { error: 'Erro ao verificar status da sincronização' };[m
[32m+[m[32m  } catch (error: any) {[m
[32m+[m[32m    console.error('Error checking sync status (Eklesia):', error, error.stack);[m
[32m+[m[32m    return { error: `Erro ao verificar status da sincronização: ${error.message}` };[m
   }[m
 }[m
 [m
[33mcommit 4c97b9beaea0f91ea201e9c7d1d817c6b33851f8[m
Author: Rodrigo.Miranda <Rodrigo.Miranda@nzd.local>
Date:   Sat Mar 21 15:28:57 2026 -0300

    feat: reestruturacao da tela de fator r e ajuste eklesia progress

[1mdiff --git a/src/components/integrations/eklesia/transactions-manager.tsx b/src/components/integrations/eklesia/transactions-manager.tsx[m
[1mindex 0ec16c3..be686cc 100644[m
[1m--- a/src/components/integrations/eklesia/transactions-manager.tsx[m
[1m+++ b/src/components/integrations/eklesia/transactions-manager.tsx[m
[36m@@ -69,24 +69,23 @@[m [mexport function TransactionsManager({ companyId }: TransactionsManagerProps) {[m
     if (isSyncing && !syncResult) {[m
       setSyncProgress(0);[m
       [m
[31m-      // Ajuste dinâmico baseado na quantidade de registros para a curva de progresso[m
       const pendingCount = syncStats?.pending || 10;[m
[32m+[m[32m      const estimatedTimeMs = Math.min(Math.max(2000, pendingCount * 300), 45000);[m[41m [m
       [m
[31m-      // Quanto mais registros, menor o passo (avança mais lentamente)[m
[31m-      // Base latency da API do Questor é alta (envio de arquivo), então a curva [m
[31m-      // começa em 0.06 (para poucos registros) e diminui até 0.01 (para muitos registros).[m
[31m-      const decayFactor = Math.max(0.01, 0.06 - (pendingCount * 0.00005));[m
[32m+[m[32m      const intervalMs = 100;[m
[32m+[m[32m      const totalStepsTo90 = estimatedTimeMs / intervalMs;[m
[32m+[m[32m      const incrementPerStep = 90 / totalStepsTo90;[m
 [m
       interval = setInterval(() => {[m
         setSyncProgress((prev) => {[m
[31m-          // Curva assintótica: calcula a distância até 99% e avança uma fração dessa distância.[m
[31m-          // Isso cria um efeito visual onde o progresso começa rápido e vai desacelerando [m
[31m-          // suavemente, nunca parando de se mover, mas sem bater no 100% até a resposta chegar.[m
[31m-          const remaining = 99 - prev;[m
[31m-          const step = remaining * decayFactor;[m
[31m-          return prev + step;[m
[32m+[m[32m          if (prev < 90) {[m
[32m+[m[32m            return Math.min(90, prev + incrementPerStep);[m
[32m+[m[32m          } else if (prev < 98) {[m
[32m+[m[32m            return prev + 0.05;[m
[32m+[m[32m          }[m
[32m+[m[32m          return prev;[m
         });[m
[31m-      }, 300); // Atualiza a cada 300ms[m
[32m+[m[32m      }, intervalMs);[m
     } else if (syncResult) {[m
       setSyncProgress(100);[m
     }[m
[33md55929f[m fix: date shift issue and zen api cleanup
[33m699f797[m fix: date formatting and disable sync buttons on empty grid
[33mced9154[m fix: apply ILIKE with parameters and fix layout resizing
[33m4d1f558[m fix: resolve date RangeError on eklesia import and fix invalid date processing
[33mda765f1[m fix: resolve RangeError Invalid time value in Eklesia and Enuves CSV imports
[33m015f5f9[m fix: ajustes na importacao csv eklesia e filtros de empresas
[33m6555efd[m Fix RangeError: Invalid time value in Eklesia CSV import
[33m0ad7ae5[m feat: switch eklesia import from pdf to csv
[33m9f25571[m fix: resolve erro 413 (payload too large) ao importar relatorio 1504 (maior que 1MB) e corrige validacao do ts
[33m9e1ea4c[m fix: resolve erro na importacao do relatorio 1504 do Eklesia no ambiente de producao e remove zeros a esquerda das categorias/contas
[33m141b61f[m feat: concluir integracao de parser PDF do Eklesia e seletor global
[33mbfd5df6[m fix(eklesia): add missing exportTransactionsCsv function
[33m3e64cd3[m feat(eklesia): batch delete and pdf fix
[33m68333fc[m fix(eklesia): use full description for accounts list format (e.g. 10-CAIXA GERAL)
[33m7bef6fd[m fix(eklesia): support new accounts list format (reduced-code code-desc)
[33m52df278[m fix(eklesia): improve accounts parsing with bold fallback and robust regex
[33md61b2d1[m fix(eklesia): add auto-migration to apply schema updates on startup and resolve import errors
[33m8f9cb4f[m fix(eklesia): increase description column size to TEXT and remove truncation
[33m24c10ab[m fix(eklesia): fix save categories logic and truncate fields
[33m24469c1[m fix(eklesia): remove leading zeros from main code in PDF import
[33m355e64b[m fix(eklesia): remove zeros a esquerda do codigo reduzido na importacao pdf
[33mee39d26[m feat: implement Eklesia PDF import with bold detection and block logic
[33mbde2813[m chore: comment pdf parser usage to diagnose build failure
[33m62c2e9e[m fix: update eklesia import logic and clean build
[33m8388a94[m fix: remove prisma from build script and dependencies, update pdf parser
[33mb4a0ad9[m feat: Implement PDF import with bold detection and fix build
[33m596fa6b[m Enhance PDF parsing with bold detection for Eklesia accounts filtering
[33m5872de2[m Implement PDF import for Eklesia categories/accounts and refine transaction parsing
[33md5173f5[m feat(eklesia): port features from enuves (batch delete, sync, filters)
[33mb9eb4f5[m fix: change const to let for text variable in eklesia integration
[33m9b3a2f7[m fix: update eklesia integration to use pdf-parse wrapper
[33m5380382[m fix: hydration mismatch in ChartCard
[33m3665493[m chore-alinhar-prod
[33m1ce3cb8[m feat:Questor_integration_and_Enuves_fixes
[33mcommit 699f797d2de64efaff95e7c809eec7a8b1d709f1[m
Author: Rodrigo.Miranda <Rodrigo.Miranda@nzd.local>
Date:   Fri Mar 20 15:28:59 2026 -0300

    fix: date formatting and disable sync buttons on empty grid

[1mdiff --git a/src/app/actions/integrations/eklesia.ts b/src/app/actions/integrations/eklesia.ts[m
[1mindex c539e66..c60541c 100644[m
[1m--- a/src/app/actions/integrations/eklesia.ts[m
[1m+++ b/src/app/actions/integrations/eklesia.ts[m
[36m@@ -1549,8 +1549,14 @@[m [mexport async function exportTransactionsCsv([m
         const header = ['Data', 'Categoria', 'Natureza', 'Histórico', 'Valor', 'Conta', 'Cód. Conta'];[m
         const rows = transactions.map(t => [[m
             t.date ? (() => {[m
[31m-              const d = new Date(t.date + 'T12:00:00');[m
[31m-              return !isNaN(d.getTime()) ? d.toLocaleDateString('pt-BR') : '';[m
[32m+[m[32m              try {[m
[32m+[m[32m                const parsed = t.date instanceof Date[m[41m [m
[32m+[m[32m                  ? t.date[m[41m [m
[32m+[m[32m                  : (typeof t.date === 'string' ? (t.date.includes('T') ? new Date(t.date) : new Date(t.date + 'T12:00:00')) : new Date(t.date));[m
[32m+[m[32m                return !isNaN(parsed.getTime()) ? parsed.toLocaleDateString('pt-BR') : '';[m
[32m+[m[32m              } catch (e) {[m
[32m+[m[32m                return '';[m
[32m+[m[32m              }[m
             })() : '',[m
             t.category_name || '',[m
             t.category_nature || '',[m

                   SSUUMMMMAARRYY OOFF LLEESSSS CCOOMMMMAANNDDSS

      Commands marked with * may be preceded by a number, _N.
      Notes in parentheses indicate the behavior if _N is given.
      A key preceded by a caret indicates the Ctrl key; thus ^K is ctrl-K.

  h  H                 Display this help.
  q  :q  Q  :Q  ZZ     Exit.
 ---------------------------------------------------------------------------

                           MMOOVVIINNGG

  e  ^E  j  ^N  CR  *  Forward  one line   (or _N lines).
  y  ^Y  k  ^K  ^P  *  Backward one line   (or _N lines).
  ESC-j             *  Forward  one file line (or _N file lines).
  ESC-k             *  Backward one file line (or _N file lines).
  f  ^F  ^V  SPACE  *  Forward  one window (or _N lines).
  b  ^B  ESC-v      *  Backward one window (or _N lines).
  z                 *  Forward  one window (and set window to _N).
  w                 *  Backward one window (and set window to _N).
  ESC-SPACE         *  Forward  one window, but don't stop at end-of-file.
  ESC-b             *  Backward one window, but don't stop at beginning-of-file.
  d  ^D             *  Forward  one half-window (and set half-window to _N).
  u  ^U             *  Backward one half-window (and set half-window to _N).
  ESC-)  RightArrow *  Right one half screen width (or _N positions).
  ESC-(  LeftArrow  *  Left  one half screen width (or _N positions).
  ESC-}  ^RightArrow   Right to last column displayed.
  ESC-{  ^LeftArrow    Left  to first column.
  F                    Forward forever; like "tail -f".
  ESC-F                Like F but stop when search pattern is found.
  ESC-f                Like F but ring the bell when search pattern is found.
  r  ^R  ^L            Repaint screen.
  R                    Repaint screen, discarding buffered input.
        ---------------------------------------------------
        Default "window" is the screen height.
        Default "half-window" is half of the screen height.
 ---------------------------------------------------------------------------

                          SSEEAARRCCHHIINNGG

  /_p_a_t_t_e_r_n          *  Search forward for (_N-th) matching line.
  ?_p_a_t_t_e_r_n          *  Search backward for (_N-th) matching line.
  n                 *  Repeat previous search (for _N-th occurrence).
  N                 *  Repeat previous search in reverse direction.
  ESC-n             *  Repeat previous search, spanning files.
  ESC-N             *  Repeat previous search, reverse dir. & spanning files.
  ^O^N  ^On         *  Search forward for (_N-th) OSC8 hyperlink.
  ^O^P  ^Op         *  Search backward for (_N-th) OSC8 hyperlink.
  ^O^L  ^Ol            Jump to the currently selected OSC8 hyperlink.
  ESC-u                Undo (toggle) search highlighting.
  ESC-U                Clear search highlighting.
  &_p_a_t_t_e_r_n          *  Display only matching lines.
        ---------------------------------------------------
		Search is case-sensitive unless changed with -i or -I.
        A search pattern may begin with one or more of:
        ^N or !  Search for NON-matching lines.
        ^E or *  Search multiple files (pass thru END OF FILE).
        ^F or @  Start search at FIRST file (for /) or last file (for ?).
        ^K       Highlight matches, but don't move (KEEP position).
        ^R       Don't use REGULAR EXPRESSIONS.
        ^S _n     Search for match in _n-th parenthesized subpattern.
        ^W       WRAP search if no match found.
        ^L       Enter next character literally into pattern.
 ---------------------------------------------------------------------------

                           JJUUMMPPIINNGG

  g  <  ESC-<       *  Go to first line in file (or line _N).
  G  >  ESC->       *  Go to last line in file (or line _N).
  p  %              *  Go to beginning of file (or _N percent into file).
  t                 *  Go to the (_N-th) next tag.
  T                 *  Go to the (_N-th) previous tag.
  {  (  [           *  Find close bracket } ) ].
  }  )  ]           *  Find open bracket { ( [.
  ESC-^F _<_c_1_> _<_c_2_>  *  Find close bracket _<_c_2_>.
  ESC-^B _<_c_1_> _<_c_2_>  *  Find open bracket _<_c_1_>.
        ---------------------------------------------------
        Each "find close bracket" command goes forward to the close bracket 
          matching the (_N-th) open bracket in the top line.
        Each "find open bracket" command goes backward to the open bracket 
          matching the (_N-th) close bracket in the bottom line.

  m_<_l_e_t_t_e_r_>            Mark the current top line with <letter>.
  M_<_l_e_t_t_e_r_>            Mark the current bottom line with <letter>.
  '_<_l_e_t_t_e_r_>            Go to a previously marked position.
  ''                   Go to the previous position.
  ^X^X                 Same as '.
  ESC-m_<_l_e_t_t_e_r_>        Clear a mark.
        ---------------------------------------------------
        A mark is any upper-case or lower-case letter.
        Certain marks are predefined:
             ^  means  beginning of the file
             $  means  end of the file
 ---------------------------------------------------------------------------

                        CCHHAANNGGIINNGG FFIILLEESS

  :e [_f_i_l_e]            Examine a new file.
  ^X^V                 Same as :e.
  :n                *  Examine the (_N-th) next file from the command line.
  :p                *  Examine the (_N-th) previous file from the command line.
  :x                *  Examine the first (or _N-th) file from the command line.
  ^O^O                 Open the currently selected OSC8 hyperlink.
  :d                   Delete the current file from the command line list.
  =  ^G  :f            Print current file name.
 ---------------------------------------------------------------------------

                    MMIISSCCEELLLLAANNEEOOUUSS CCOOMMMMAANNDDSS

  -_<_f_l_a_g_>              Toggle a command line option [see OPTIONS below].
  --_<_n_a_m_e_>             Toggle a command line option, by name.
  __<_f_l_a_g_>              Display the setting of a command line option.
  ___<_n_a_m_e_>             Display the setting of an option, by name.
  +_c_m_d                 Execute the less cmd each time a new file is examined.

  !_c_o_m_m_a_n_d             Execute the shell command with $SHELL.
  #_c_o_m_m_a_n_d             Execute the shell command, expanded like a prompt.
  |XX_c_o_m_m_a_n_d            Pipe file between current pos & mark XX to shell command.
  s _f_i_l_e               Save input to a file.
  v                    Edit the current file with $VISUAL or $EDITOR.
  V                    Print version number of "less".
 ---------------------------------------------------------------------------

                           OOPPTTIIOONNSS

        Most options may be changed either on the command line,
        or from within less by using the - or -- command.
        Options may be given in one of two forms: either a single
        character preceded by a -, or a name preceded by --.

  -?  ........  --help
                  Display help (from command line).
  -a  ........  --search-skip-screen
                  Search skips current screen.
  -A  ........  --SEARCH-SKIP-SCREEN
                  Search starts just after target line.
  -b [_N]  ....  --buffers=[_N]
                  Number of buffers.
  -B  ........  --auto-buffers
                  Don't automatically allocate buffers for pipes.
  -c  ........  --clear-screen
                  Repaint by clearing rather than scrolling.
  -d  ........  --dumb
                  Dumb terminal.
  -D xx_c_o_l_o_r  .  --color=xx_c_o_l_o_r
                  Set screen colors.
[33mcommit 7abe2f5a9bad39af179ebf7e03dbb9569a572701[m[33m ([m[1;36mHEAD[m[33m -> [m[1;32mmain[m[33m)[m
Author: Rodrigo.Miranda <Rodrigo.Miranda@nzd.local>
Date:   Sun Mar 22 17:35:07 2026 -0300

    fix: correcao na exibicao de data no modulo enuves

[1mdiff --git a/src/app/actions/integrations/enuves.ts b/src/app/actions/integrations/enuves.ts[m
[1mindex 2513c7b..e8a9ee8 100644[m
[1m--- a/src/app/actions/integrations/enuves.ts[m
[1m+++ b/src/app/actions/integrations/enuves.ts[m
[36m@@ -438,9 +438,26 @@[m [mexport async function getTransactions([m
 [m
     query += ` ORDER BY t.date DESC`;[m
 [m
[31m-    const transactions = await db.prepare(query).all(...params) as Transaction[];[m
[32m+[m[32m    const transactions = await db.prepare(query).all(...params) as any[];[m
     [m
[31m-    return transactions;[m
[32m+[m[32m    return transactions.map(t => {[m
[32m+[m[32m      let dateStr = null;[m
[32m+[m[32m      if (t.date) {[m
[32m+[m[32m        if (t.date instanceof Date) {[m
[32m+[m[32m           const y = t.date.getFullYear();[m
[32m+[m[32m           const m = String(t.date.getMonth() + 1).padStart(2, '0');[m
[32m+[m[32m           const d = String(t.date.getDate()).padStart(2, '0');[m
[32m+[m[32m           dateStr = `${y}-${m}-${d}`;[m
[32m+[m[32m        } else {[m
[32m+[m[32m           dateStr = String(t.date).split('T')[0];[m
[32m+[m[32m        }[m
[32m+[m[32m      }[m
[32m+[m[32m      return {[m
[32m+[m[32m        ...t,[m
[32m+[m[32m        date: dateStr,[m
[32m+[m[32m        created_at: t.created_at ? (t.created_at instanceof Date ? t.created_at.toISOString() : String(t.created_at)) : null,[m
[32m+[m[32m      };[m
[32m+[m[32m    }) as Transaction[];[m
   } catch (error) {[m
     console.error('Error fetching transactions:', error);[m
     return [];[m
  -e  -E  ....  --quit-at-eof  --QUIT-AT-EOF
                  Quit at end of file.
  -f  ........  --force
                  Force open non-regular files.
  -F  ........  --quit-if-one-screen
                  Quit if entire file fits on first screen.
  -g  ........  --hilite-search
                  Highlight only last match for searches.
  -G  ........  --HILITE-SEARCH
                  Don't highlight any matches for searches.
  -h [_N]  ....  --max-back-scroll=[_N]
                  Backward scroll limit.
  -i  ........  --ignore-case
                  Ignore case in searches that do not contain uppercase.
  -I  ........  --IGNORE-CASE
                  Ignore case in all searches.
  -j [_N]  ....  --jump-target=[_N]
                  Screen position of target lines.
  -J  ........  --status-column
                  Display a status column at left edge of screen.
  -k _f_i_l_e  ...  --lesskey-file=_f_i_l_e
                  Use a compiled lesskey file.
  -K  ........  --quit-on-intr
                  Exit less in response to ctrl-C.
  -L  ........  --no-lessopen
                  Ignore the LESSOPEN environment variable.
  -m  -M  ....  --long-prompt  --LONG-PROMPT
                  Set prompt style.
  -n .........  --line-numbers
                  Suppress line numbers in prompts and messages.
  -N .........  --LINE-NUMBERS
                  Display line number at start of each line.
  -o [_f_i_l_e] ..  --log-file=[_f_i_l_e]
                  Copy to log file (standard input only).
  -O [_f_i_l_e] ..  --LOG-FILE=[_f_i_l_e]
                  Copy to log file (unconditionally overwrite).
  -p _p_a_t_t_e_r_n .  --pattern=[_p_a_t_t_e_r_n]
                  Start at pattern (from command line).
  -P [_p_r_o_m_p_t]   --prompt=[_p_r_o_m_p_t]
                  Define new prompt.
  -q  -Q  ....  --quiet  --QUIET  --silent --SILENT
                  Quiet the terminal bell.
  -r  -R  ....  --raw-control-chars  --RAW-CONTROL-CHARS
                  Output "raw" control characters.
  -s  ........  --squeeze-blank-lines
                  Squeeze multiple blank lines.
  -S  ........  --chop-long-lines
                  Chop (truncate) long lines rather than wrapping.
  -t _t_a_g  ....  --tag=[_t_a_g]
                  Find a tag.
  -T [_t_a_g_s_f_i_l_e] --tag-file=[_t_a_g_s_f_i_l_e]
                  Use an alternate tags file.
  -u  -U  ....  --underline-special  --UNDERLINE-SPECIAL
                  Change handling of backspaces, tabs and carriage returns.
  -V  ........  --version
                  Display the version number of "less".
  -w  ........  --hilite-unread
                  Highlight first new line after forward-screen.
  -W  ........  --HILITE-UNREAD
                  Highlight first new line after any forward movement.
  -x [_N[,...]]  --tabs=[_N[,...]]
                  Set tab stops.
  -X  ........  --no-init
                  Don't use termcap init/deinit strings.
  -y [_N]  ....  --max-forw-scroll=[_N]
                  Forward scroll limit.
  -z [_N]  ....  --window=[_N]
                  Set size of window.
  -" [_c[_c]]  .  --quotes=[_c[_c]]
                  Set shell quote characters.
  -~  ........  --tilde
                  Don't display tildes after end of file.
  -# [_N]  ....  --shift=[_N]
                  Set horizontal scroll amount (0 = one half screen width).

                --autosave=[_m_/_!_*]
                  Actions which cause the history file to be saved.
                --exit-follow-on-close
                  Exit F command on a pipe when writer closes pipe.
                --file-size
                  Automatically determine the size of the input file.
                --follow-name
                  The F command changes files if the input file is renamed.
                --form-feed
                  Stop scrolling when a form feed character is reached.
                --header=[_L[,_C[,_N]]]
                  Use _L lines (starting at line _N) and _C columns as headers.
                --incsearch
                  Search file as each pattern character is typed in.
                --intr=[_C]
                  Use _C instead of ^X to interrupt a read.
                --lesskey-context=_t_e_x_t
                  Use lesskey source file contents.
                --lesskey-src=_f_i_l_e
                  Use a lesskey source file.
                --line-num-width=[_N]
                  Set the width of the -N line number field to _N characters.
                --match-shift=[_N]
                  Show at least _N characters to the left of a search match.
                --modelines=[_N]
                  Read _N lines from the input file and look for vim modelines.
                --mouse
                  Enable mouse input.
                --no-edit-warn
                  Don't warn when using v command on a file opened via LESSOPEN.
                --no-keypad
                  Don't send termcap keypad init/deinit strings.
                --no-histdups
                  Remove duplicates from command history.
                --no-number-headers
                  Don't give line numbers to header lines.
                --no-paste
                  Ignore pasted input.
                --no-search-header-lines
                  Searches do not include header lines.
                --no-search-header-columns
                  Searches do not include header columns.
                --no-search-headers
                  Searches do not include header lines or columns.
                --no-vbell
                  Disable the terminal's visual bell.
                --redraw-on-quit
                  Redraw final screen when quitting.
                --rscroll=[_C]
                  Set the character used to mark truncated lines.
                --save-marks
                  Retain marks across invocations of less.
                --search-options=[EFKNRW-]
                  Set default options for every search.
                --show-preproc-errors
                  Display a message if preprocessor exits with an error status.
                --proc-backspace
                  Process backspaces for bold/underline.
                --PROC-BACKSPACE
                  Treat backspaces as control characters.
                --proc-return
                  Delete carriage returns before newline.
                --PROC-RETURN
                  Treat carriage returns as control characters.
                --proc-tab
                  Expand tabs to spaces.
                --PROC-TAB
                  Treat tabs as control characters.
                --status-col-width=[_N]
                  Set the width of the -J status column to _N characters.
                --status-line
                  Highlight or color the entire line containing a mark.
                --use-backslash
                  Subsequent options use backslash as escape char.
                --use-color
                  Enables colored text.
                --wheel-lines=[_N]
                  Each click of the mouse wheel moves _N lines.
                --wordwrap
                  Wrap lines at spaces.


 ---------------------------------------------------------------------------

                          LLIINNEE EEDDIITTIINNGG

        These keys can be used to edit text being entered 
        on the "command line" at the bottom of the screen.

 RightArrow ..................... ESC-l ... Move cursor right one character.
 LeftArrow ...................... ESC-h ... Move cursor left one character.
 ctrl-RightArrow  ESC-RightArrow  ESC-w ... Move cursor right one word.
 ctrl-LeftArrow   ESC-LeftArrow   ESC-b ... Move cursor left one word.
 HOME ........................... ESC-0 ... Move cursor to start of line.
 END ............................ ESC-$ ... Move cursor to end of line.
 BACKSPACE ................................ Delete char to left of cursor.
 DELETE ......................... ESC-x ... Delete char under cursor.
 ctrl-BACKSPACE   ESC-BACKSPACE ........... Delete word to left of cursor.
 ctrl-DELETE .... ESC-DELETE .... ESC-X ... Delete word under cursor.
 ctrl-U ......... ESC (MS-DOS only) ....... Delete entire line.
 UpArrow ........................ ESC-k ... Retrieve previous command line.
 DownArrow ...................... ESC-j ... Retrieve next command line.
 TAB ...................................... Complete filename & cycle.
 SHIFT-TAB ...................... ESC-TAB   Complete filename & reverse cycle.
 ctrl-L ................................... Complete filename, list all.
warning: in the working copy of 'src/components/imposto-renda/ir-details.tsx', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/src/components/imposto-renda/ir-details.tsx b/src/components/imposto-renda/ir-details.tsx[m
[1mindex c9e6aca..e3978cd 100644[m
[1m--- a/src/components/imposto-renda/ir-details.tsx[m
[1m+++ b/src/components/imposto-renda/ir-details.tsx[m
[36m@@ -188,37 +188,47 @@[m [mexport function IRDetails({ declaration, interactions }: IRDetailsProps) {[m
   };[m
 [m
   // Status buttons visibility logic[m
[31m-  const getAvailableActions = () => {[m
[32m+[m[32m  const getAllActions = () => {[m
     const status = declaration.status;[m
[31m-    const actions: { label: string, target: IRStatus, justify: boolean, color: string }[] = [];[m
[32m+[m[32m    const actions: { label: string, target: IRStatus, justify: boolean, color: string, disabled: boolean }[] = [[m
[32m+[m[32m      { label: 'Iniciar', target: 'Iniciado', justify: false, color: 'bg-blue-900', disabled: true },[m
[32m+[m[32m      { label: 'Pendente', target: 'Pendente', justify: true, color: 'bg-red-600', disabled: true },[m
[32m+[m[32m      { label: 'Validada', target: 'Validada', justify: false, color: 'bg-yellow-500', disabled: true },[m
[32m+[m[32m      { label: 'Transmitida', target: 'Transmitida', justify: false, color: 'bg-orange-500', disabled: true },[m
[32m+[m[32m      { label: 'Processada', target: 'Processada', justify: false, color: 'bg-green-600', disabled: true },[m
[32m+[m[32m      { label: 'Malha Fina', target: 'Malha Fina', justify: false, color: 'bg-pink-600', disabled: true },[m
[32m+[m[32m      { label: 'Retificadora', target: 'Retificadora', justify: false, color: 'bg-purple-600', disabled: true },[m
[32m+[m[32m      { label: 'Reabrir', target: 'Reaberta', justify: false, color: 'bg-blue-400', disabled: true },[m
[32m+[m[32m      { label: 'Cancelar', target: 'Cancelada', justify: true, color: 'bg-slate-900', disabled: true },[m
[32m+[m[32m    ];[m
 [m
     if (status === 'Não Iniciado') {[m
[31m-      actions.push({ label: 'Iniciar', target: 'Iniciado', justify: false, color: 'bg-blue-900' });[m
[31m-      actions.push({ label: 'Cancelar', target: 'Cancelada', justify: true, color: 'bg-slate-900' });[m
[31m-    } else if (status === 'Iniciado' || status === 'Reaberta') { // Reaberta functions like Iniciado based on logic flow[m
[31m-      actions.push({ label: 'Validada', target: 'Validada', justify: false, color: 'bg-yellow-500' });[m
[31m-      actions.push({ label: 'Pendente', target: 'Pendente', justify: true, color: 'bg-red-600' });[m
[31m-      actions.push({ label: 'Cancelar', target: 'Cancelada', justify: true, color: 'bg-slate-900' });[m
[32m+[m[32m      actions.find(a => a.label === 'Iniciar')!.disabled = false;[m
[32m+[m[32m      actions.find(a => a.label === 'Cancelar')!.disabled = false;[m
[32m+[m[32m    } else if (status === 'Iniciado' || status === 'Reaberta') {[m
[32m+[m[32m      actions.find(a => a.label === 'Validada')!.disabled = false;[m
[32m+[m[32m      actions.find(a => a.label === 'Pendente')!.disabled = false;[m
[32m+[m[32m      actions.find(a => a.label === 'Cancelar')!.disabled = false;[m
     } else if (status === 'Pendente') {[m
[31m-      actions.push({ label: 'Validada', target: 'Validada', justify: false, color: 'bg-yellow-500' });[m
[31m-      actions.push({ label: 'Cancelar', target: 'Cancelada', justify: true, color: 'bg-slate-900' });[m
[32m+[m[32m      actions.find(a => a.label === 'Validada')!.disabled = false;[m
[32m+[m[32m      actions.find(a => a.label === 'Cancelar')!.disabled = false;[m
     } else if (status === 'Validada') {[m
[31m-      actions.push({ label: 'Transmitida', target: 'Transmitida', justify: false, color: 'bg-orange-500' });[m
[31m-      actions.push({ label: 'Pendente', target: 'Pendente', justify: true, color: 'bg-red-600' });[m
[31m-      actions.push({ label: 'Cancelar', target: 'Cancelada', justify: true, color: 'bg-slate-900' });[m
[32m+[m[32m      actions.find(a => a.label === 'Transmitida')!.disabled = false;[m
[32m+[m[32m      actions.find(a => a.label === 'Pendente')!.disabled = false;[m
[32m+[m[32m      actions.find(a => a.label === 'Cancelar')!.disabled = false;[m
     } else if (status === 'Transmitida') {[m
[31m-      actions.push({ label: 'Processada', target: 'Processada', justify: false, color: 'bg-green-600' });[m
[31m-      actions.push({ label: 'Malha Fina', target: 'Malha Fina', justify: false, color: 'bg-pink-600' });[m
[31m-      actions.push({ label: 'Retificadora', target: 'Retificadora', justify: false, color: 'bg-purple-600' });[m
[32m+[m[32m      actions.find(a => a.label === 'Processada')!.disabled = false;[m
[32m+[m[32m      actions.find(a => a.label === 'Malha Fina')!.disabled = false;[m
[32m+[m[32m      actions.find(a => a.label === 'Retificadora')!.disabled = false;[m
     } else if (status === 'Processada' || status === 'Malha Fina') {[m
[31m-      actions.push({ label: 'Retificadora', target: 'Retificadora', justify: false, color: 'bg-purple-600' });[m
[32m+[m[32m      actions.find(a => a.label === 'Retificadora')!.disabled = false;[m
     } else if (status === 'Retificadora') {[m
[31m-      actions.push({ label: 'Transmitida', target: 'Transmitida', justify: false, color: 'bg-orange-500' });[m
[31m-      actions.push({ label: 'Processada', target: 'Processada', justify: false, color: 'bg-green-600' });[m
[31m-      actions.push({ label: 'Malha Fina', target: 'Malha Fina', justify: false, color: 'bg-pink-600' });[m
[31m-      actions.push({ label: 'Retificadora', target: 'Retificadora', justify: false, color: 'bg-purple-600' });[m
[32m+[m[32m      actions.find(a => a.label === 'Transmitida')!.disabled = false;[m
[32m+[m[32m      actions.find(a => a.label === 'Processada')!.disabled = false;[m
[32m+[m[32m      actions.find(a => a.label === 'Malha Fina')!.disabled = false;[m
[32m+[m[32m      actions.find(a => a.label === 'Retificadora')!.disabled = false;[m
     } else if (status === 'Cancelada') {[m
[31m-      actions.push({ label: 'Reabrir', target: 'Reaberta', justify: false, color: 'bg-blue-400' });[m
[32m+[m[32m      actions.find(a => a.label === 'Reabrir')!.disabled = false;[m
     }[m
 [m
     return actions;[m
[36m@@ -387,15 +397,19 @@[m [mexport function IRDetails({ declaration, interactions }: IRDetailsProps) {[m
               <CardDescription>Ações disponíveis para o fluxo</CardDescription>[m
             </CardHeader>[m
             <CardContent className="flex flex-col gap-2">[m
[31m-              {getAvailableActions().map((action, idx) => ([m
[32m+[m[32m              {getAllActions().map((action, idx) => ([m
                 <Button [m
                   key={idx} [m
                   onClick={() => handleStatusChange(action.target, action.justify)} [m
[31m-                  disabled={loading} [m
[31m-                  className="w-full justify-start hover:bg-muted bg-transparent text-foreground border shadow-sm" [m
[32m+[m[32m                  disabled={loading || action.disabled}[m[41m [m
[32m+[m[32m                  className={`w-full justify-start border shadow-sm transition-all ${[m
[32m+[m[32m                    action.disabled[m[41m [m
[32m+[m[32m                      ? 'opacity-50 cursor-not-allowed bg-muted/50 text-muted-foreground'[m[41m [m
[32m+[m[32m                      : 'hover:bg-muted bg-transparent text-foreground'[m
[32m+[m[32m                  }`}[m
                   variant="outline"[m
                 >[m
[31m-                  <span className={`w-3 h-3 rounded-full ${action.color} mr-2 shadow-sm`}></span> [m
[32m+[m[32m                  <span className={`w-3 h-3 rounded-full ${action.color} mr-2 shadow-sm ${action.disabled ? 'opacity-50' : ''}`}></span>[m[41m [m
                   {action.label}[m
                 </Button>[m
               ))}[m
