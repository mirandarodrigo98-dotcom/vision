[1mdiff --git a/src/app/actions/integrations/eklesia.ts b/src/app/actions/integrations/eklesia.ts[m
[1mindex d61f690..bdbd6a5 100644[m
[1m--- a/src/app/actions/integrations/eklesia.ts[m
[1m+++ b/src/app/actions/integrations/eklesia.ts[m
[36m@@ -55,15 +55,15 @@[m [mexport async function getCategories([m
 [m
     if (filters) {[m
       if (filters.code) {[m
[31m-        query += ` AND code LIKE ?`;[m
[32m+[m[32m        query += ` AND code ILIKE ?`;[m
         params.push(`%${filters.code}%`);[m
       }[m
       if (filters.description) {[m
[31m-        query += ` AND description LIKE ?`;[m
[32m+[m[32m        query += ` AND description ILIKE ?`;[m
         params.push(`%${filters.description}%`);[m
       }[m
       if (filters.integration_code) {[m
[31m-        query += ` AND integration_code LIKE ?`;[m
[32m+[m[32m        query += ` AND integration_code ILIKE ?`;[m
         params.push(`%${filters.integration_code}%`);[m
       }[m
       if (filters.nature && filters.nature !== 'all') {[m
[36m@@ -1322,15 +1322,18 @@[m [mexport async function parseEklesiaCsv(formData: FormData, companyId: string) {[m
       const conta = row[5] || '';[m
 [m
       // Column G - Histórico[m
[31m-      let historico = row[6] || '';[m
[31m-      if (!historico) {[m
[31m-        historico = categoria;[m
[31m-      }[m
[32m+[m[32m      const historicoG = row[6] || '';[m
[32m+[m[32m      let historico = historicoG ? `${categoria} - ${historicoG}`.trim() : categoria;[m
 [m
       // Column H - Valor[m
       let valorStr = row[7] || '0';[m
[31m-      valorStr = valorStr.replace(/-/g, '').replace(/\./g, '').replace(',', '.');[m
[31m-      const valor = Math.abs(parseFloat(valorStr));[m
[32m+[m[32m      const isNegative = valorStr.includes('-');[m
[32m+[m[32m      valorStr = valorStr.replace(/-/g, '').replace(/R\$\s?/g, '').replace(/\./g, '').replace(',', '.');[m
[32m+[m[32m      const valor = parseFloat(valorStr);[m
[32m+[m
[32m+[m[32m      if (valor === 0 || isNaN(valor)) {[m
[32m+[m[32m        continue; // Ignora se estiver zerado[m
[32m+[m[32m      }[m
 [m
       // Match Category[m
       let categoryId = null;[m
[36m@@ -1367,6 +1370,8 @@[m [mexport async function parseEklesiaCsv(formData: FormData, companyId: string) {[m
         ignoredTransactions.push({[m
             date: convertDate(dateStr),[m
             value: valor,[m
[32m+[m[32m            isNegative: isNegative,[m
[32m+[m[32m            originalCategory: categoria,[m
             reason: 'Categoria não encontrada ou não cadastrada no sistema',[m
             line: `${historico} - Categoria Original: ${categoria}`[m
         });[m
